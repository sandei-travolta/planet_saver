import 'package:flutter/material.dart';
class DisposalSection extends StatelessWidget {
  const DisposalSection({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
        flex: 10,
        child: Container(

        )
    );
  }
}
